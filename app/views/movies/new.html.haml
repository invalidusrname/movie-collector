.firstpost
  .firstposttitle
    %h3 New movie
  .firstpostcontent
    - form_for(@movie) do |form|
      = render :partial => "form", :object => form
      %p
        = form.submit 'Create', :onclick => 'submitMovie(this.form);'

    = link_to 'Back', movies_path
