%h2 
  =h @movie.title 

%p
  = link_to image_tag(@movie.image), @movie.image_link

%p
  %b Format:
  =h @movie.format 

%p
  %b Private:
  =h @movie.private 

%p
  %b ASIN:
  =h @movie.asin 

%p
  %b Borrowed By:
  =h @movie.borrower.email if @movie.borrower 

= link_to 'Edit', edit_movie_path(@movie)  |
= link_to 'Back', movies_path