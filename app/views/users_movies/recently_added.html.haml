%h1= @title = 'Recently Added'

/ %h2= @title = 'Recently Added'

.post
  #recently_added
    %ul
      - @users_movies.each do |users_movie|
        - movie = users_movie.movie
        %li= link_to image_tag(thumbnail(movie), :title => movie.title), users_movie_path(users_movie)
